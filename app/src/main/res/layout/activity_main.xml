<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:text="@string/plane1"
        android:onClick="plane1"
        android:id="@+id/plane1"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@id/plane1"
        android:text="@string/plane2"
        android:onClick="plane2"
        android:id="@+id/plane2"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@id/plane2"
        android:text="@string/connect"
        android:onClick="connect"
        android:id="@+id/connect"
        />]

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@id/connect"
        android:text="@string/disconnect"
        android:onClick="disconnect"
        android:id="@+id/disconnect"
        />

    <ajwilson.nedcontroller.JoystickView
        android:id="@+id/joystickView"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_toRightOf="@id/plane1">
    </ajwilson.nedcontroller.JoystickView>
</RelativeLayout>
